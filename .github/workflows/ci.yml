# SPDX-License-Identifier: Apache-2.0

name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  quality:
    name: Python ${{ matrix.python-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.12", "3.13", "3.14"]
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip
      - name: Install package and test tools
        run: python -m pip install --disable-pip-version-check '.[test]'
      - name: Check formatting
        run: ruff format --check src tests scripts
      - name: Lint
        run: ruff check src tests scripts
      - name: Type check
        run: mypy src
      - name: Check public contracts
        run: python scripts/verify_contracts.py
      - name: Test
        run: pytest --cov=meridian_storage.object_common --cov-report=term-missing --cov-report=xml

  package:
    name: Reproducible package
    needs: quality
    runs-on: ubuntu-latest
    steps:
      - name: Check out source
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Set up Python
        uses: actions/setup-python@a26af69be951a213d495a4c3e4e4022e16d87065 # v5
        with:
          python-version: "3.13"
          cache: pip
      - name: Install package and build tools
        run: python -m pip install --disable-pip-version-check '.[test]'
      - name: Fix reproducible-build timestamp
        run: echo "SOURCE_DATE_EPOCH=$(git show -s --format=%ct HEAD)" >> "$GITHUB_ENV"
      - name: Build twice
        run: |
          python -m build --no-isolation --outdir .artifacts/first
          python -m build --no-isolation --outdir .artifacts/second
      - name: Compare builds
        run: python scripts/compare_artifacts.py .artifacts/first .artifacts/second
      - name: Verify package contents
        run: python scripts/verify_artifacts.py .artifacts/first/*
      - name: Generate SPDX SBOM
        run: |
          python scripts/generate_sbom.py --output ".artifacts/meridian-storage-object-common-1.0.0.spdx.json" .artifacts/first/*
      - name: Clean-install wheel with released predecessors
        run: |
          python -m venv .artifacts/venv
          WHEEL_PATH=$(find .artifacts/first -name '*.whl' -type f -print -quit)
          .artifacts/venv/bin/python -m pip install --disable-pip-version-check meridian-storage-core==1.0.0 meridian-storage-semantics==1.0.0 "$WHEEL_PATH"
          cd /tmp
          "$GITHUB_WORKSPACE/.artifacts/venv/bin/python" -I -c 'from meridian_storage.spi.discovery import CATALOG_ENTRY_POINT_GROUP, discover_components; result = discover_components(CATALOG_ENTRY_POINT_GROUP, (), component_id=lambda provider: provider.catalog_name); provider = result.components["object"]; assert len(provider.manifest().operations) == 8 and provider.create_surface().catalog_name == "object"'
      - name: Upload verified artifacts
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4
        with:
          name: meridian-storage-object-common-${{ github.sha }}
          path: |
            .artifacts/first/*
            .artifacts/meridian-storage-object-common-*.spdx.json
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 30
